// Online C++ compiler to run C++ program online
#include <bits/stdc++.h>

using namespace std;
int main() {
    // Write C++ code here
    
    // not my code - just Hello world :)
    /*std::cout << "Hello world!";*/
    
    // below lines @ the beginning of the code make input & output more efficient
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    // cin, cout
    /*int a,b;
    string x;
    cin >> a >> b >> x;

    int c,d;
    string y;
    c=a,d=b,y=x;
    
    cout << c << d << y << endl;*/
    
    // scanf,printf
    /*scanf("%d %d",&a,&b);
    printf("%d %d\n",a,b);*/
    
    // read a whole line from the input, possibly containing spaces
    /*string s;
    getline(cin,s);
    cout << s.substr(3,5) << endl;
    cout << "done !!!" << endl;*/
    
    // if the amount of data is unknown, the following loop is useful
    /*string x;
    while (cin >> x) {
        //cout << x << endl;
        //any process
    }*/
    
    // files used for input & output
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
    return 0;
}